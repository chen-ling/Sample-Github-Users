<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RegularText" parent="android:Widget.TextView">
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">@dimen/sp_14</item>
    </style>
    <style name="SingleLineRegularText" parent="RegularText">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
    </style>
    <style name="DetailText" parent="RegularText">
        <item name="android:layout_marginStart">@dimen/dp_20</item>
    </style>

    <style name="DetailIcon">
        <item name="android:layout_marginTop">@dimen/dp_20</item>
        <item name="android:tint">@color/black</item>
    </style>

</resources>
